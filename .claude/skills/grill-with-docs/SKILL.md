---
name: grill-with-docs
description: A relentless INTERVIEW (via /domain-modeling) that challenges and sharpens an already-proposed plan or design, recording the outcome as ADRs/glossary entries as it goes. This is not a tool for verifying external API/provider documentation — for that, use WebSearch/WebFetch (and an official MCP if one exists) directly, optionally followed by this skill only to formalize the resulting decision as an ADR. Manual-only: it actively interrogates the user, so it must never fire silently on a message match.
disable-model-invocation: true
---

Run a `/grilling` session, using the `/domain-modeling` skill.
