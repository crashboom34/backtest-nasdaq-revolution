# Issue tracker: GitHub Issues

Issues and specs for this repo live as **GitHub Issues** in
[`crashboom34/backtest-nasdaq-revolution`](https://github.com/crashboom34/backtest-nasdaq-revolution).
Use the `gh` CLI for all operations.

## Conventions

- **Create an issue**: `gh issue create --title "..." --body "..."`. Use a heredoc for multi-line bodies.
- **Read an issue**: `gh issue view <number> --comments`, filtering comments by `jq` and also fetching labels.
- **List issues**: `gh issue list --state open --json number,title,body,labels,comments --jq '[.[] | {number, title, body, labels: [.labels[].name], comments: [.comments[].body]}]'` with appropriate `--label` and `--state` filters.
- **Comment on an issue**: `gh issue comment <number> --body "..."`
- **Apply / remove labels**: `gh issue edit <number> --add-label "..."` / `--remove-label "..."`
- **Close**: `gh issue close <number> --comment "..."`

Infer the repo from `git remote -v` — `gh` does this automatically when run inside a clone.

## Rules specific to this project

- Specs and tickets generated by `to-spec` / `to-tickets` **must reference the GitHub issue number(s)** they came from or that they create.
- Branches and commits should reference the relevant issue number when one exists (e.g. `fix/backtest-slippage-#42`, `Fixes #42` in a commit body).
- **No issue is closed without verifying the work it describes was actually done** — re-read the issue's acceptance criteria against the diff/tests before closing, don't close on trust alone.

## Pull requests as a triage surface

**PRs as a request surface: no.** _(Left at the skill's default. Flip to `yes` in this file if the project later wants external PRs treated as feature requests; `/triage`, if installed, reads this flag.)_

## When a skill says "publish to the issue tracker"

Create a GitHub issue with `gh issue create`.

## When a skill says "fetch the relevant ticket"

Run `gh issue view <number> --comments`.

## Status

Configured manually as part of installing the Matt Pocock engineering skills (see `docs/adr/0001-shared-skills-for-claude-code-and-codex.md`). Re-run `/setup-matt-pocock-skills` (or its manual equivalent) if the tracker choice ever changes.
