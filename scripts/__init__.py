"""scripts/ — utilitaires opérationnels exécutables directement (`python scripts/<nom>.py`).

Ce fichier `__init__.py` rend le dossier importable comme package (ex.
`from scripts.create_walk_forward_validation_split_plan import ...` depuis les tests) sans changer
l'usage existant : chaque script reste également exécutable en standalone, inchangé.
"""
